</div> <footer class="bg-dark text-light text-center py-4 mt-auto">
        <p class="mb-0">© 2026 AutoRent Pro. Uy tín - Chất lượng - Nhanh chóng.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
    const user = JSON.parse(localStorage.getItem('user'));
    const token = localStorage.getItem('token');
    const menu = document.getElementById('authMenu');

    if (user && token) {
        let specialLink = '';

        // 1. Nếu là Chủ xe -> Hiện nút Quản lý xe
        if (user.role === 'OWNER') {
            specialLink = `<li class="nav-item"><a class="nav-link text-warning fw-bold" href="/owner/dashboard">👑 Quản lý xe</a></li>`;
        } 
        // 2. Nếu là Admin -> Hiện nút Admin Panel (BẠN ĐANG THIẾU ĐOẠN NÀY)
        else if (user.role === 'ADMIN') {
            specialLink = `<li class="nav-item"><a class="nav-link text-danger fw-bold" href="/admin/dashboard">🛡️ Admin Panel</a></li>`;
        }
        
        menu.innerHTML = `
            <li class="nav-item"><span class="nav-link text-white">Xin chào, <b>${user.fullName}</b></span></li>
            ${specialLink} 
            <li class="nav-item"><a class="nav-link" href="/my-bookings">Lịch sử thuê</a></li>
            <li class="nav-item"><a class="nav-link btn btn-danger btn-sm text-white ms-2" href="#" onclick="logout()">Đăng xuất</a></li>
        `;
    } else {
        menu.innerHTML = `
            <li class="nav-item"><a class="nav-link" href="/login">Đăng nhập</a></li>
            <li class="nav-item"><a class="nav-link btn btn-warning text-dark ms-2" href="/register">Đăng ký</a></li>
        `;
    }

    function logout() {
        localStorage.clear();
        window.location.href = '/login';
    }
</script>
</body>
</html>